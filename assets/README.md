The contents of this directory are accessible at the site root.

Anything directly inside of this current folder will be shadowed by the `/:game`
route inside of leptos and cannot be read.

For data that is always used for all games, 
such as `ynoengine(-simd).{js,wasm}`,
that is put in the `data` directory.
